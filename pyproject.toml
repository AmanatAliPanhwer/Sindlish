[project]
name = "sindlish"
version = "0.5.0"
description = "Sindlish is a interpreted programming language Inspired by Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
