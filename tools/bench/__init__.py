"""Sindlish benchmark harness.

A standalone, driver-based benchmarking tool that measures the cold-start
wall time of language runtimes (old Sindlish, new Sindlish, Python, or any
shell command) on the same logical benchmark cases, plus an optional
in-process per-pipeline-stage breakdown for the current implementation.
"""
